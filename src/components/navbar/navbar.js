import React from "react";
import style from "./navbar.module.css";

const NavBar = () =>{
    return(
        <header>

            <nav id={style["navbar"]}>

                <p>navbar</p>

            </nav>

        </header>
    );
};

export default NavBar;